/* Здесь вы напишете основную часть стилей страницы.

Будьте внимательны! Для корректной работы скриптов на этом сайте нужно, чтобы в HTML некоторые классы были названы особым образом:
✦ like-icon — для svg-иконки анимированного сердца
✦ card__like-button — для кнопки Like рядом с иконкой
✦ card__icon-button — для кнопки, оборачивающей иконку
✦ is-liked — для обозначения состояния лайкнутой иконки в виде сердца
✦ button__text — для обозначения текстового элемента внутри кнопки

*/

.page {
  font-family: var(--font-main);
  font-variation-settings: var(--text-weight);
  min-inline-size: 320px;
  min-block-size: 100dvb;
  background-image: var(--gradient-horizontal), var(--gradient-vertical),
    var(--gradient-color);
  background-size: cover;
  background-attachment: fixed;
}


.background-container {
  position: relative;
  background-color: #fff;
}

.background-container::before {
  content: "";
  position: absolute;
  inset: 0;
  border: 2px solid black;
  pointer-events: none;
  
}

/* .background-container {
  background-color: #fff;
  border: 2px solid var(--border-color);
  outline: 2px solid black;
  outline-offset: -2px;
  inline-size: clamp(375px, 100%, 700px);
} */

.header {
  display: flex;
  min-block-size: clamp(527px, 100%, 566px);
  justify-content: center;
  align-items: center;
}

.header__block {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  block-size: 327px;
  gap: 20px;
}

.header__block-html {
  font-family: var(--font-accent);
  font-weight: var(--text-weight);
  font-size: 49px;
}

.header__block-title {
  font-family: var(--font-accent);
  font-weight: var(--text-weight);
  font-size: 14px;
  text-transform: uppercase;
}

.section {
  display: flex;
  flex-direction: column;
  gap: 50px;
  align-items: center;
}

.section__article-title {
  font-size: var(--text-size);
  font-variation-settings: var(--text-header-weight);
  line-height: 120%;
  margin: 6px 12px;
}

.section__article-wrapper {
  position: relative;
}

.section__article-img {
  display: block;
  width: 100%;
  aspect-ratio: 371 / 371;
  object-fit: cover;
  background-position: center;
  background-repeat: no-repeat;
  outline: 2px solid black;
  outline-offset: -2px;
}

.watermark {
  position: absolute;
  top: 27px;
  right: 25px;
  color: rgb(255 255 255 / 0.7);
  text-shadow: 2px 2px 4px rgb(0 0 0 / 0.5);
}

.watermark-text {
  font-family: var(--font-accent);
  font-size: 14px;
  font-weight: 400;
  text-transform: uppercase;
  color: #000;
  -webkit-text-stroke: 1px #fff;
  opacity: 0.5;
}

.section__article-text-block {
  display: grid;
  gap: 25px;
  margin: 23px 27px;
}

.section__article-text {
  font-size: var(--text-size);
  font-variation-settings: var(--text-post-weight);
}

.section__article-buttons {
  display: flex;
  justify-content: flex-end;
  margin: 0 27px 23px 0;
  padding-top: 3px;
}

.card__icon-button {
  background-color: transparent;
  border: 0;
}

.card__like-button {
  background-color: transparent;
  border: 2px solid var(--border-color);
  width: 130px;
  height: 38px;
  padding-top: 5px;
}

.button__text {
  font-family: var(--font-accent);
  font-variation-settings: var(--text-weight);
  font-size: 14px;
}
